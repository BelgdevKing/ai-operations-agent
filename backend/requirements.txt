# Runtime dependencies (Python 3.12)
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
# EmailStr validation
email-validator>=2.2,<3.0

# Authentication. Argon2id password hashing (OWASP's first choice) and
# JWT signing/verification. Neither is implemented by hand.
argon2-cffi>=23.1,<26.0
pyjwt>=2.10,<3.0
sqlalchemy[asyncio]>=2.0.36,<3.0
alembic>=1.14,<2.0
# zoneinfo has no time zone database on Windows, and slim Linux images may
# lack one too. Alembic needs it for the UTC timestamps in migration names.
tzdata>=2024.2
asyncpg>=0.30,<1.0
redis>=5.2,<7.0
