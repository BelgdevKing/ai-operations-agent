"""Tool registry.

Planned home of the tools an agent may call, each declared with a JSON schema,
a handler, a sensitivity level and the roles allowed to invoke it. Nothing is
implemented yet - see docs/architecture.md section 7.
"""
